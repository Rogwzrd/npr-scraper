<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
    <a class="navbar-brand" href="/">NPR Scraper</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="/">Saved Articles <span class="sr-only">(current)</span></a>
            </li>
        </ul>
    </div>
</nav>
<div id="savedArticles" class="container">
    {{#each article}}
        <div class="card text-light bg-dark mb-3 low-opacity">
            <div class="card-header">
                <a href="{{link}}" target="_blank" class="link-style align-left"><h4>{{title}}</h4></a>
                <button class="btn btn-danger align-right saveArticle" data-id="{{_id}}">Save</button>
            </div>
            <div class="card-body">
                <blockquote class="blockquote mb-0">
                    <p>{{teaser}}</p>
                </blockquote>
            </div>
        </div>
    {{/each}}
</div>